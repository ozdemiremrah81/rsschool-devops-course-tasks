apiVersion: v1
kind: Service
metadata:
  name: {{ include "emrah-mern.fullname" . }}
  labels:
    {{- include "emrah-mern.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "emrah-mern.selectorLabels" . | nindent 4 }}
